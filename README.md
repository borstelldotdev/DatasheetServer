# Datasheet server
A simple server for datasheets

# Installation
Clone the repository:
```
git clone https://github.com/borstelldotdev/DatasheetServer.git
cd DatasheetServer
```

Install dependencies:

```
pip install -r requirements.txt
```

Run the server:

```
python app.py
```

# Configuring
See `config.json`
